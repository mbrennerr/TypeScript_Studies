let fullName: [string, string] = ["M", "Brenner"];
let person: [string, number] = ["MB", 35];
let car: [string, string, number] = ["Ford","F400",10];

console.log(fullName);
console.log(person);
console.log(car);

/*Permite declarar um conjunto de  vaalores cuja ordem e tipo dos valores são fixas;*/